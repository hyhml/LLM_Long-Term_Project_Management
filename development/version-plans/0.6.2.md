# Version plan: 0.6.2 skill discovery safety

- Status: Implementation in progress
- Change weight: Runtime implementation followed by stable release
- Requirements: `CHILD-012`, `FLOW-005`, `ISOL-007`, `QUAL-002`
- Feedback: two externally retained, verified reports with accepted synthetic derivatives

## Scope

- Report whether a generated project-local child can be discovered from the current Codex working directory; otherwise require a new session rooted at the project.
- Keep transaction candidates data-only and validate them through an ephemeral complete project assembled outside the discovered child tree.
- Remove new committed candidates while preserving plans and receipts.
- Provide preview-bound, user-approved cleanup for verified committed candidates left by older framework versions.
- Preserve entry protocol 1, existing project-data schemas, project-local child ownership, and package formats.

## Validation and release

1. Demonstrate the two minimized synthetic regressions on v0.6.1 behavior.
2. Run affected bootstrap and transaction tests after implementation.
3. Run the complete developer suite once after runtime source freezes.
4. Validate development and built runtime skills and one child generated from the exact artifact.
5. Publish v0.6.2 from the whitelist-built runtime, then issue external resolution receipts.

No new ADR is required: this version restores the already accepted project-local discovery, transaction, and isolation contracts without changing their architecture.

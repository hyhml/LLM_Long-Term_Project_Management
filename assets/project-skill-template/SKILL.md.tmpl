---
name: @@SKILL_NAME@@
description: @@DESCRIPTION_JSON@@
---

# @@PROJECT_NAME_HEADING@@

This is the thin, explicitly invoked entry for project `@@PROJECT_ID@@`, generated and governed by `long-term-project-manager`.

## Load every time

1. Read `framework/instance.json`, `project-instructions.md`, and `views/project-map.json`.
2. Locate the explicitly installed, compatible `long-term-project-manager`; read its `SKILL.md` and high-priority framework map rules. Shared protocols and tools come from that total skill, not from copied project-local framework files.
3. Verify that the canonical child root, immutable `project_id`, project revision, entry protocol, and managed adapter hashes form a compatible project binding. If compatibility is uncertain, permit inspection but block formal writes and route to the total skill's upgrade procedure.
4. Treat `views/project-map.json` as a directly loaded, derived navigation view. `state/`, `records/`, and `sources/` are authoritative; `work/` is pending. Never change project state through `views/` or `index/`.
5. Read formal detail only for the current nodes or task. Do not load another project's state unless the user explicitly requests cross-project coordination.

## Project binding

- This ordinary conversation binds to this one project. If it is already bound to another `project_id`, do not switch silently: obtain explicit confirmation or recommend a new conversation.
- State the project name and `project_id` before a formal write, handoff export, integration, or migration.
- Work in one total-framework role: `maintainer`, `explorer`, or `integrator`, and follow the matching shared protocol.
- All reads and writes default to this child root. A mismatched package, transaction, identity, or destination fails closed.
- Cross-project work requires the total skill's explicit read-mostly coordination mode. It never creates a combined transaction.
- If the user raises a problem that may involve the framework, or this child observes a reusable framework failure, route to the total skill's consent-based feedback protocol. Treat it as a candidate classification. Ordinary current-context feedback needs one final preview approval; reading new private or external material first needs one bounded collection approval.

## Project-owned boundary

- Preserve `project-instructions.md` across framework updates.
- Never let a framework adapter update rewrite `state/`, `records/`, `sources/`, `work/`, project artifacts, packages, or user-owned instructions.
- Project-data schema migration is a separate itemized, user-confirmed transaction with its own validation and receipt.
